import { ROLES } from "./roles"

export const ACTIONS = {
  DELETE_FILE: "DELETE_FILE",
  CREATE_FILE: "CREATE_FILE",
  UPDATE_FILE: "UPDATE_FILE",
  VIEW_FILE: "VIEW_FILE",
} as const;

export const PERMISSIONS = {
  [ACTIONS.DELETE_FILE]: [ROLES.ADMIN],
  [ACTIONS.CREATE_FILE]: [ROLES.ADMIN, ROLES.EMPLOYEE, ROLES.MANAGER],
  [ACTIONS.UPDATE_FILE]: [ROLES.ADMIN, ROLES.EMPLOYEE, ROLES.MANAGER],
  [ACTIONS.VIEW_FILE]: [ROLES.ADMIN, ROLES.EMPLOYEE, ROLES.MANAGER],
}
